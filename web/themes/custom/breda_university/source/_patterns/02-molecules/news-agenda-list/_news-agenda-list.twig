{#
/**
 * @file
 * news-agenda-list
 *
 * Available variables for news-agenda-list:
 *    - news_agenda_list.variable_name [variable_type]: Description of the variable. Example below.
 *    - news_agenda_list.invert [bool]: When set, colors will be inverted for light display on dark background.
 */
#}

<ul class="news-agenda-list">
    {% for item in news_agenda_items %}
        {{ item }}
    {% endfor %}
</ul>
